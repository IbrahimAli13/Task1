<%= form_with(model: carinfo) do |form| %>
  <% if carinfo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(carinfo.errors.count, "error") %> prohibited this carinfo from being saved:</h2>

      <ul>
        <% carinfo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :car_id, style: "display: block" %>
    <%= form.select :car_id, Car.all.pluck(:brand_name ,:id) %>
  </div>

  <div>
    <%= form.label :plate, style: "display: block" %>
    <%= form.text_field :plate %>
  </div>

  <div>
    <%= form.label :owner, style: "display: block" %>
    <%= form.text_field :owner %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
